#version 400
in vec3 v_Color;
in vec2 v_TexCoords;

out vec4 f_Color;

uniform float u_Radius;

void main() {
	float t = step(length(v_TexCoords - vec2(0.5)), 0.5);
  f_Color = t * vec4(v_Color, 1.0);
}